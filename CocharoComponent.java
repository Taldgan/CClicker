import java.awt.Font;

public class CocharoComponent extends InteractiveComponent {
	public CocharoComponent(String path, int x, int y, int w, int h, String text, Font font, int tx, int ty, String id) {
		super( path, x, y, w, h, text, font, tx, ty, id);
	}
}
