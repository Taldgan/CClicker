import java.awt.Font;

public class InteractiveText extends InteractiveComponent {
	public InteractiveText(String text, Font font, int tx, int ty, int w, int h, String id, int r, int g, int b, State st) {
		super(text, font, tx, ty, w, h, id, r, g, b, st);
	}
}
